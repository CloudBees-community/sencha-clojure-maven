(ns sencha.test
  (:use [sencha.core])
  (:use [sencha.database])
  (:use [clojure.test]))

(deftest replace-me 
  (is false "You might want to write some tests for your application, I would. Maybe."))
